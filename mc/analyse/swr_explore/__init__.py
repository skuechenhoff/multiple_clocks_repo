"""Exploratory ripple-content analyses.

None of this is in the reported result -- that is `scripts/swr_content_main.py`.
Each module here is one analysis with its own constants and its own `main()`,
kept separate rather than merged into one file so that a constant in one cannot
silently change another. `scripts/swr_content_explore.py` is the single entry
point and dispatches to them.
"""
